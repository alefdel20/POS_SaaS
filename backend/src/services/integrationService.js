async function queueGoogleSheetsImport() {
  return { message: "TODO: Google Sheets import hook pending MVP+1" };
}

async function queueExcelImport() {
  return { message: "TODO: Excel import hook pending MVP+1" };
}

async function syncWithN8n() {
  return { message: "TODO: n8n sync hook pending MVP+1" };
}

module.exports = {
  queueGoogleSheetsImport,
  queueExcelImport,
  syncWithN8n
};
